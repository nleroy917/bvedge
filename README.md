# bvedge
BED-file embeddings at the edge. An experimental system for generating embeddings of genomic region embeddings using lightweight models that can run on consumer hardware in real-time.

Trying my hand at [candle](https://github.com/candle-rs/candle).